package org.agora.occ.enums;

/**
 * Enum representing the passenger crowding danger level at a station platform
 * or JPL.
 */
public enum DangerLevel {
    EMPTY,
    LIGHT,
    MODERATE,
    CROWDED,
    VERY_CROWDED
}
